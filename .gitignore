# 忽略所有 build 目录（编译产物，所有子项目通用）
**/build/

# 忽略 VSCode 配置中的临时文件（保留核心配置）
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json

# 忽略 C++ 编译生成的可执行文件（覆盖所有目录）
*.out          # 忽略 a.out 这类带后缀的可执行文件
*.exe          # 忽略 Windows 可执行文件（兼容跨平台）

# 忽略 src 目录下的非源码文件（核心修正）
**/src/*                  # 先忽略 src 下所有文件
!**/src/*.cpp             # 保留 .cpp 源码
!**/src/*.h               # 保留 .h 头文件
!**/src/*.hpp             # 保留 .hpp 头文件

# 额外保险：明确忽略常见可执行文件名（可选，确保万无一失）
**/src/main
**/src/a.out

# 忽略系统临时文件
.*.swp        # Vim 临时文件
.DS_Store     # Mac 系统文件